# Counter

## Running locally on physical iPhone

- `npm run prebuild`
- npm run start
- ensure server is running `development` mode
- scan QR Code to open on phyical iPhone

NB not possible to run in Expo Go due to `react-native-volume-manager` dependency
